# Public frontend config (safe to expose in the browser bundle).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Server-only secret used by scripts/add-recipe.mjs. NEVER prefix with VITE_ (would ship to the browser).
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxx
